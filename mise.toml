[settings]
auto_install = false

[tasks.ts]
description = "Run TypeScript backend"
dir = "."
# env = { PIPE_BOLT_PROJECT_CONFIG = "test-mqtt/config/pipe-bolt.project.json" }
run = "pnpm dev:dev"

[tasks.vue]
description = "Run Vue frontend"
dir = "apps/web"
run = "pnpm dev"

[tasks.dev]
description = "Run all services"
depends = ["ts", "vue"]